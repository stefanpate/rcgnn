defaults:
  - filepaths: base
  - data: sprhea_n_100_arc
  - model: rc_agg
  - training: dev # base
  - _self_

exp: dev # null

hydra:
  output_subdir: null # Redundant w/ mlflow
  run:
    dir: ${filepaths.scratch} # Redundant w/ mlflow