output_root: /home/stef/quest_data/hiec/results/cv
seed: 1234
dataset: sprhea
toc: v3_folded_test
neg_multiple: 1
split_strategy: random
n_splits: 5
split_bound: 100
subdir_patt: ${.dataset}_${.toc}/${.split_strategy}_${.split_bound}/nm${.neg_multiple}_${.n_splits}fold

hydra:
  run:
    dir: ${output_root}/${subdir_patt}
  job:
    chdir: True