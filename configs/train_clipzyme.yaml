defaults:
  - filepaths: base
  - data: sprhea_rcmcs
  - model: clipzyme
  - training: base
  - _self_

exp: debug_clip

hydra:
  sweeper:
    params:
      data.split_idx: range(${data.n_splits})

  sweep:
    dir: ${filepaths.results}/predictions
    subdir: .
  run:
    dir: ${filepaths.results}/predictions
  job:
    chdir: True